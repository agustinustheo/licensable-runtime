# Environment variables for Docker deployment

# NestJS API Configuration
NODE_ENV=production
PORT=3000

# Database Configuration (PostgreSQL)
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=license_db

# Substrate Node Configuration
# Using --dev mode for local testing
# For production, remove --dev and configure proper chain spec